export declare class Controller {
    constructor();
    action1(min: number, max: number): number;
    action2(min: number, max: number): Promise<number>;
    action3(min: number, max: number): Promise<number>;
    action4(min: number, max: number): Promise<number>;
}
